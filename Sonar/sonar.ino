int trigPin = 12;
int echoPin = 11;
int travleTime;

void setup() {
  // pinMode(trigPin, OUTPUT);
  // pinMode(echoPin, INPUT);
  // Serial.begin(9600);
}
void loop() {
  // digitalWrite(trigPin, LOW);
  // delayMicroseconds(10);
  // digitalWrite(trigPin, HIGH);
  // delayMicroseconds(10);
  // digitalWrite(trigPin, LOW);
  // travleTime = pulseIn(echoPin, HIGH);
  // Serial.println(travleTime);
  // delay(50);
}